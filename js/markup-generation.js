//Функция для отрисовки 1го объявления

function createCard (ad) {
   // получение ДОМ элемента карточки
}

const card = document.querySelector('#card');
const clonedCard = card.cloneNode(true);
console.log(clonedCard);
